# Tools Component-Ablation Evidence Log Snippets

- 2026-06-11: completed Tools test C-CRP signal-row generation on `pony-rec-gpu`; `test_ccrp_signal_rows.csv` has 1,010,000 rows, provenance reports `n_events=10000`, `parse_failure_count=0`, and source audit reports `status=recomputable_signal_rows`, duplicate keys `0`, and candidate-key coverage `1.0`.
- 2026-06-11: ran `scripts/misc/main_select_ccrp_variant_on_valid.py` with preregistered main C-CRP config (`eta=1.0`, weights `0.5,0.3,0.2`, `tie_break_seed=20260607`) and `--import_scores`; importer reported `score_coverage_rate=1.000000` and `NDCG@10=0.16949770813562767`.
- 2026-06-11: reran only the same-candidate importer on the frozen `ccrp_selected_test_scores.csv` with baseline `tools_ccrp_component_full`, `status_label=same_schema_internal_ablation`, and `--tie_break_seed 20260607`; metrics stayed unchanged and score coverage remained `1.000000`. As with Home, this component-row import intentionally omits `method_provenance_path` so the component-ablation status is not overridden by selector provenance; `ccrp_internal_provenance.json` remains in the package for the module audit.
- 2026-06-11: ran `scripts/analysis/main_build_ccrp_component_ablation_summary.py` for Tools; builder returned `ok=true`, ablations `full`, `without_boundary_uncertainty`, `without_calibration_gap`, `without_evidence_support`, `without_counterevidence`, and `without_risk_penalty`, with `failures=[]`.
- 2026-06-11: synchronized the lightweight local evidence package tables, figures, selected config/provenance, and component summary files; server `predictions/rank_predictions.jsonl`, `ccrp_selected_test_scored_rows.csv`, and full `ccrp_selected_test_scores.csv` were not copied into the local package.
