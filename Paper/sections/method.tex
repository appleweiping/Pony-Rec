\section{Method}
\label{sec:method}

\subsection{Problem Formulation}

Given a user $u$ with interaction history $H_u$ and a candidate set
$C_u=\{c_1,\ldots,c_n\}$, the same-candidate reranking task is to order all
candidates so that the held-out next item is ranked as highly as possible. In
our formal setting, $n=101$ for every test event: one positive item and 100
negatives. Every compared method must produce one finite score per
$(u,c_i)$ pair under the same key schema.

The core object in \method{} is a calibrated candidate relevance posterior
$p_i=P(y_i=1\mid H_u,c_i)$ and an uncertainty estimate $U_i\in[0,1]$ attached
to that posterior. The final ranking score is risk-adjusted:
\begin{equation}
    r_i = p_i(1-U_i)^\eta ,
    \label{eq:risk-score}
\end{equation}
where $\eta$ is fixed or selected using validation data only. When $\eta=0$,
the score reduces to the calibrated posterior. This special case is important:
our hyperparameter analysis treats it as a valid setting, and the paper does
not claim that a positive risk exponent is always necessary.

\subsection{Task-Grounded Candidate Evidence}

For each user-candidate pair, the LLM is asked to evaluate the candidate under
the next-interaction task rather than under a generic similarity task. The
structured output contains a raw relevance probability, a calibrated relevance
probability, evidence support, counterevidence strength, and a short rationale.
The prompt does not reveal the positive item index or test label.

This evidence format separates three roles that are often conflated in LLM
recommendation: the model's relevance estimate, the support for that estimate,
and the amount of unresolved risk. The final score exporter keeps all candidate
keys aligned with the same-candidate task so downstream metrics can be audited
without relying on candidate order or missing-score imputation.

\subsection{Uncertainty Decomposition}

\method{} computes event-level and candidate-level uncertainty from four
signals:

\begin{itemize}
    \item \textbf{Boundary ambiguity:} candidates close to the decision
    boundary are treated as less certain than clearly high- or low-relevance
    candidates.
    \item \textbf{Calibration gap:} disagreement between raw and calibrated
    relevance estimates indicates posterior instability.
    \item \textbf{Evidence insufficiency:} weak support from user history and
    item evidence increases uncertainty.
    \item \textbf{Counterevidence:} explicit evidence against relevance raises
    the risk attached to an otherwise high score.
\end{itemize}

These components are deliberately auditable. Section~\ref{sec:results}
reports leave-one-component-out diagnostics rather than assuming that every
component is beneficial. In fact, the current evidence shows that several
components are neutral or redundant under the frozen configuration.

\subsection{Validation Discipline}

All method choices that can affect the final score are frozen or selected
before test evaluation. Validation rows choose among allowed score modes,
weights, and $\eta$ settings; test rows are used only for final metric
reporting, paired tests, observation figures, component diagnostics, and
hyperparameter sensitivity plots. The test hyperparameter sweeps reported in
the paper are descriptive stability checks and cannot change the method.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{../outputs/summary/paper_critical/framework_overview/framework_overview.pdf}
\caption{Framework overview. \method{} converts same-candidate user-item
events into task-grounded LLM evidence, calibrates candidate relevance
posteriors, estimates uncertainty, and applies risk-adjusted reranking. The
official baselines and \method{} share the same candidate rows, metric importer,
and evidence gates.}
\Description{Pipeline diagram showing same-candidate task construction, LLM
signal extraction, calibration, C-CRP uncertainty, risk-adjusted ranking, and
shared evidence gates for official baselines and C-CRP.}
\label{fig:framework}
\end{figure*}
