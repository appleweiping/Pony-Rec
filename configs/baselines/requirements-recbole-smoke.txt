# Optional pins for RecBole 1.2.x smoke runs (install into your venv when running baselines).
# Not installed by default; see docs/PILOT_RECBOLE_SMOKE.md.
recbole>=1.2,<1.3
numpy<2
setuptools<81
scipy>=1.10,<1.11
